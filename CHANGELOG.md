# 更新日志

## 未发布

## v1.3.4

- 新增独立信息展板“状态核心”，与数据条、仪表和液位容器同级，旧 `dashboardStyle: "liquid"` 配置继续保持兼容。
- 状态核心首批提供断续双轨、错位光栅、斜切晶片、磁悬液珠、晶格液桥、裂变液滴链、断桥矩阵和三通道晶格 8 种形态。
- 首页快捷面板和主题管理页均可调整核心形态、结构密度、断裂程度、光晕强度、动效强度及延迟上限；本机样式和全站默认均可保存。
- 状态核心使用无大外壳布局，百分比和具体值保持纯文字；高占用会逐步向告警红色升温，低速非零数据保留最小可见状态。
- 动效仅作用于当前进度边缘，并使用 `content-visibility`、布局隔离和减少动画模式控制多节点性能；修复手机端上/下行小数值被挤断的问题。

## v1.3.3

- 移除数据条图形外层的白色圆角轨道框，取消背景、边框、圆角、内边距和裁切。
- 所有数据条细节样式不再绘制灰白色未使用轨道，经典点阵、极光丝带、电路线条、霓虹脉冲与均衡器方块均只保留彩色动态图形。

## v1.3.2

- 全面移除信息展板具体数值下方的圆角底板：弧光仪表、全环仪表、指针仪表与全部液位容器均改为纯文字详情。
- 数据条同步移除 CPU、内存、硬盘、负载、上下行与流量额度详情框，所有信息展板统一为无背景、无边框、无圆角的读数样式。

## v1.3.1

- 数据条百分比读数明确统一为透明纯文字样式，移除可能由卡片预设叠加的背景、边框、圆角和阴影框。
- 移除数据条详情数值槽的白色椭圆底板，CPU 核心、内存、硬盘和流量额度等具体值改为直接显示，同时保留跑马灯动画与轻量文字阴影。

## v1.3.0-p9

- 移除弧光仪表、全环仪表、指针仪表和液位容器百分比读数外层的白色椭圆背景、边框与阴影框，统一改为纯文字显示。
- 保留轻量文字阴影和原有居中层级，确保复杂仪表、液体与指针背景下读数仍然清楚，且不会被指针遮挡。

## v1.3.0-p8

- 统一重做五类信息展板的视觉层级：数据条、流量统计和 Ping 区域使用同源仪器单元，标题、读数、具体值与动态轨道更容易扫读。
- 弧光、全环和指针仪表新增渐变进度、实时端点、内层表盘与底部数值槽；指针仪表补齐 11 级刻度、金属轴心和遮挡保护，不再让指针压住中心百分比。
- 修复半环个性预设忽略内外半径的问题，碎片轨道、脉冲齿环、液态胶囊、电路星轨、双轨错位、极光丝带环和断点扫描现在能形成真正不同的层次。
- 优化 8 种液位容器的实体感、液体对比和读数清晰度，修正扫描扇区旋转偏移；横向胶囊、分段胶囊和椭圆透镜改用真实横向比例。
- 保留水波、液面、气泡、扫描节点等轻量动态，同时将指针刻度合并为两条 SVG 路径并避免恢复高成本滤镜；桌面与 `390px` 手机宽度均完成无横向溢出检查。

## v1.3.0-p7

- “欢迎访问”访客信息弹窗新增本地默认主题头像，使用独立的 192×192 轻量资源，不再显示成浅色背景下不明显的空白线框图标。
- 头像使用圆形裁切、细边框和轻量外环阴影，能跟随弹窗玻璃背景保持清晰层次。
- 保留原有用户图标作为资源加载失败回退；头像完全随主题包本地分发，不请求第三方图片地址。

## v1.3.0-p6

- 将 GitHub 源码从 `v1.2.9-p1` 全量同步到当前 `v1.3.0` 系列，包含首页搜索与快捷筛选、健康摘要、异常集中、节点拓扑、共同故障分析、JSON / CSV 快照导出、节点 message、访客信息弹窗和真实交互地球。
- 优化交互地球实时更新：CPU、内存、流量等与地球无关的数据刷新不再重复重建地区标记、脉冲和 HTML 标签，只有节点地区、名称、数量或在线状态变化时才同步 WebGL 数据。
- 合并 `ResizeObserver` 的重复尺寸通知，地球宽高未变化时不再重复调整渲染器，降低窗口缩放、响应式切换和页面布局稳定阶段的重绘。
- 浅色 / 深色切换现在会同步更新地球纹理、材质和四组灯光强度；初次加载不再重复设置同一张地球纹理。
- 地球会监听系统“减少动态效果”设置，运行期间切换该设置也能即时停止或恢复自动旋转。
- 昼夜纹理由 4096×2048 优化为 2048×1024：两张纹理合计由约 `2.18 MB` 降至约 `0.55 MB`，单张纹理解码后的理论 RGBA 显存由约 `32 MiB` 降至约 `8 MiB`，当前最大显示尺寸下仍保留足够细节。
- GitHub Actions 新增类型检查、依赖安全审计和 ZIP 第三方许可文件校验，避免源码、版本、发布包或许可说明不同步。

## v1.3.0-p5

- 重做首页节点地理视图：参考 Glassmorphism 的真实 WebGL 地球布局，将地球放到首页顶部右侧下层，总览与快捷筛选位于左侧，节点卡片自然覆盖地球下缘。
- 地球支持自动旋转和鼠标 / 触摸拖动，节点按地区聚合为国旗标记与在线状态脉冲，并根据当前搜索和筛选结果实时更新。
- 浅色模式使用蓝色大理石纹理，深色模式使用夜景纹理，同时加入地形凹凸、水面高光和大气层效果。
- 地球模块默认关闭并保持按需加载；Three.js 与 globe.gl 不进入首屏入口包，离开视口或切换后台会暂停动画，设备像素比限制为 `1.5`。
- 优化桌面与手机布局：地球不再强行塞入矩形卡片或铺满整行，`390px` 窄屏下保持顶部展示、触摸交互和页面无横向溢出。
- 新增第三方来源与 MIT 许可说明，并随主题 ZIP 一起分发。

## v1.3.0-p4

- 访客信息开关启用后，每次进入或刷新首页都会自动弹出，不再受浏览器会话内“只显示一次”的限制。
- 弹窗提前显示加载状态，第三方 IP 接口较慢或不可用时也不会阻止弹窗出现。
- 访客网络信息新增 ISP、ASN 和 Organization 解析，服务商行会显示类似 `DMIT Cloud Services · AS906` 的信息，Organization 不同时会单独补充一行。
- 删除弹窗标题下方的“定位完成 / 正在识别”等状态文字，并删除底部“不写入主题配置”的说明文字。
- 访客弹窗从首页快捷筛选框中解耦；即使隐藏整块快捷筛选，只要访客信息开关开启，进入首页仍会自动弹出。

## v1.3.0-p3

- 修正“首页模块”开关的控制范围：原“健康摘要”开关改为“首页快捷筛选”，用于整体显示或隐藏搜索、分组、快捷筛选、健康摘要、拓扑分析、快照导出和访客信息入口所在的大框。
- 关闭首页快捷筛选后会同步清空搜索词、分组、快捷筛选条件和已展开的运维面板，避免界面隐藏后仍在后台按旧条件筛选节点。
- 首页快捷筛选外壳接入卡片外壳预设和渐变背板染色，清透玻璃、霓虹暗面、柔和彩块、极简白板和复古 CRT 均有对应外观。
- 搜索框、运维按钮和访客信息入口同步使用透明内层色块，不再出现外层已经染色但内部仍是纯白色的割裂效果。
- 保留对 `v1.3.0-p2` 的 `healthSummary` 本地配置兼容，升级后会自动迁移到新的 `explorerToolbar` 总开关。

## v1.3.0-p2

- 访客信息从工具栏内联文字改为紧凑入口和独立玻璃弹窗；每个浏览器会话自动提示一次，也可随时手动打开，并支持点击外部或按 `Esc` 关闭。
- 访客弹窗集中展示公网 IP、城市、地区、国家、运营商和设备信息；手机端固定在底部，避免受工具栏、地图层级和页面滚动影响。
- 节点地理视图参考 Glassmorphism 的真实贴图方案，改用卫星地球纹理、地区旗帜节点、在线/离线图例和编号地区列表，不再使用简略大陆线稿。
- 平铺地图与纹理地球继续按需加载；地球会根据当前可见节点自动选择观察中心，地图只统计当前搜索和筛选结果。
- “首页模块”新增健康摘要显示开关；关闭后首页移除健康摘要入口，也不会加载面板或请求健康历史。
- 健康摘要外层和内部风险色块提高透明度，并跟随卡片外壳、浅色/深色主题和渐变背板同步染色。
- 修复访客弹窗可能被后续地图层压住的问题，并核对桌面、手机布局均无页面横向溢出。

## v1.3.0-p1

- 融合新的首页运维工具区：健康摘要与异常集中、节点拓扑与共同故障分析、JSON / CSV 节点快照导出。
- 首页新增完整搜索、节点分组和快捷筛选，可按在线、离线、高负载、资源预警、流量预警、即将到期和探针消息筛选。
- 实时节点模型接入探针 `message`，方型卡片和条形卡片会在节点名称旁显示异常提示图标，健康摘要会集中列出消息。
- 负载历史、Ping 历史和首页 Ping 优先使用 `public:queryMetrics` / `public:getPingMetricStats`，旧版 Komari 自动回退到现有 records RPC / REST 接口。
- 健康摘要支持 24 小时、7 天和 30 天历史峰值；新版核心使用 Metric Store，旧核心自动使用实时状态，不阻断公共首页。
- 节点拓扑支持分组聚合、`upstream:` / `上游:` 标签关系、离线上游、同分组和同地区共同故障识别。
- 快照导出只使用当前搜索和筛选后的可见节点；CSV 内置公式注入防护、RFC 4180 转义和 UTF-8 BOM。
- 新增访客信息模块，使用多个第三方 IP 信息源自动回退，显示当前 IP、地区、运营商和浏览器设备信息。
- 新增可选节点地理视图，包含轻量轨道地球和平铺世界地图；默认关闭地图，开启后按需加载，不引入 Three.js 等大型依赖。
- “卡片与样式”新增“首页模块”页签，访客信息和地理视图均支持本机调整与管理员全站默认保存。
- 新增运维面板、地理视图和设置面板均拆分为异步 chunk；入口 gzip 约 `82.0 kB`，相对上一版约增加 `2.8 kB`。

## v1.2.9-p1

- 优化首次加载性能：快捷控制面板改为首屏空闲后异步加载，减少首页入口包体积和初始化压力。
- 首页节点卡不再逐卡订阅外观配置，改为列表层统一传入当前浅色 / 深色状态，降低大量节点时的订阅数量。
- 数据条 Canvas 共用 ResizeObserver 和 IntersectionObserver，并合并宽度测量，减少多节点页面的 observer 创建和首屏布局压力。
- 移除外观初始化阶段的重复公共配置请求，并减少首页无必要的外观配置序列化。
- 更新 AI 交接与维护说明，补齐顶部信息、性能优化和当前配置字段说明。

## v1.2.9

- 顶部信息的“总上下行流量”和“总流量速率”新增“拆开显示”开关。
- 开启后会拆成“总上传流量 / 总下载流量”和“总上行速率 / 总下行速率”独立卡片，减少大数字挤压。
- 拆分卡片继续跟随顶部信息显示开关、拖拽顺序和每行数量设置。

## v1.2.8

- 修复顶部信息中总上下行流量和总流量速率在数值较长时可能溢出到相邻卡片的问题。
- 顶部信息新增百分比条显示开关，可单独隐藏当前在线、总 CPU、总内存和总硬盘的右上角百分比与底部进度条。
- 优化顶部信息设置面板的百分比条按钮，在窄屏下会自动收敛为图标按钮。

## v1.2.7

- 主页延迟检测新增“未配置 Ping 时”展示策略，可选择自动隐藏、显示占位或仅绑定节点显示。
- 默认策略改为自动隐藏：未配置任何首页 Ping 绑定时，首页不再显示延迟 / 丢包占位；配置绑定后只在对应节点显示。
- 方型卡片、条形卡片、液位容器和仪表信息展板统一接入该策略，避免不同卡片外壳显示结果不一致。

## v1.2.5

- 撤回 `v1.2.4` 的“总价值 / 剩余价值”和在线汇率统计功能，恢复到稳定的顶部信息拖拽、分行和资源总览方案。
- 移除价值统计相关的汇率请求、缓存逻辑和价值卡片样式，避免价格周期识别不完整造成误导。

## v1.2.4

- 顶部信息新增“总价值”和“剩余价值”，会参与显示开关、拖拽排序和每行数量设置。
- 支持人民币、美元、欧元、英镑、卢布、法郎、卢比的热门币种统计，并在线换算成人民币展示。
- 汇率使用 Frankfurter 在线接口并缓存 24 小时；接口失败时会使用缓存或按原币种回退显示。
- 总价值按周期折算为月均价值，剩余价值按到期时间折算，长期或无到期节点按完整周期计入。

## v1.2.3

- 修复顶部信息拖拽排序只影响设置面板、不影响首页实际总览顺序的问题。
- 顶部总览项现在会按配置顺序写入明确的视觉排序，并在顺序变化后强制刷新总览容器。

## v1.2.2

- 顶部信息支持拖拽排序，首页快捷面板和主题管理页的全站默认设置会共用同一套排序逻辑。
- 顶部信息新增“每行数量”设置，可选择自动、2 个、3 个、4 个、5 个或 6 个一行，窄屏会自动收敛避免拥挤。
- 美化顶部信息设置面板，加入拖拽把手、分段列数按钮、开关状态圆标和更清晰的卡片层次。

## v1.2.1

- “卡片与样式”新增“顶部信息”页签，可单独控制当前时间、节点总数、在线节点、点亮地区、总上下行流量、总流量速率、总 CPU、总内存和总硬盘的显示。
- 顶部总览新增总 CPU、总内存、总硬盘统计卡，并为在线率和资源占用加入轻量进度条。
- 主题管理页的“全站默认卡片与样式”同步支持顶部信息开关，管理员保存后可作为全站默认展示。

## v1.2.0

- 流量统计在均衡器方块样式且满格时，改用 99% 的视觉填充绘制，保留最后一段动态闪烁空间。
- 新增数据条组件的 `visualFraction` 绘制比例，数值展示和真实进度仍可保持原始状态，避免影响其它数据条样式。

## v1.1.10

- 未获取到流量限额和统计规则时，流量统计固定按“合计额度”显示，已用值使用出站 + 入站总和，总量显示为 `∞`。
- 优化均衡器方块在 100% 满格时的动态表现，满格状态下仍会有亮度、光晕和强调色波动，不再看起来完全静止。

## v1.1.9-p9

- 开启“流量统计”后，即使节点没有设置流量限额，也会显示额度统计项。
- 未设置限额时总量显示为 `∞`，数据条按 100% 拉满，并使用正常状态颜色，避免被误判为超额告警。

## v1.1.9-p8

- 流量额度统计的明细文字精简为“已用 / 总量”，移除统计模式说明。
- 在信息展板为“数据条”时，数据条细节上方新增“流量统计”显示/关闭开关；本机快捷面板和全站默认设置都会保存该开关。
- 关闭流量统计后，方型卡片数据条区域不再渲染额度统计项，保持和其他数据条项目同层级控制。

## v1.1.9-p7

- 在 `v1.1.9-p6` 的同源组件改造基础上，继续清理旧的额度统计独立轨道、填充和动画样式，避免额度统计回到单独层级。
- 重新打包为新版本，避免覆盖已经生成的 `v1.1.9-p6` 测试包。

## v1.1.9-p6

- 重做流量额度统计的实现层级：额度统计不再使用独立的 `traffic-quota` 仿写样式，改为直接复用数据条的 `MetricBar` / `CanvasStrip` / `drawMarqueeStrip`。
- 额度统计现在和 CPU、内存、硬盘、负载使用同一套数据条绘制逻辑，经典点阵、极光丝带、电路线条、霓虹脉冲、均衡器方块会完全按同源组件生效。
- 额度统计改为普通 `metric-item` 层级，后续添加显示/关闭开关时可以直接按同级数据项控制渲染。

## v1.1.9-p5

- 修复流量额度统计没有跟随“数据条细节”形态的问题，额度进度槽现在会随经典点阵、极光丝带、电路线条、霓虹脉冲、均衡器方块切换对应样式。
- 额度统计接入数据条的密度、圆角、光晕和动效强度，保留稳定的 DOM/CSS 实现，不恢复 `v1.1.9-p2` 的 Canvas 额度条方案。
- 额度统计自身写入当前跑马灯样式，主页和设置预览都能按同一套样式变量渲染。

## v1.1.9-p4

- 重新统一方型卡片“数据条”的细节样式：出站 / 入站累计统计改为和 CPU、内存等数据条一致的胶囊细节样式。
- 优化流量额度统计的标题、百分比、已用 / 总量、进度槽和间距；保留稳定的 CSS 静态进度条实现，不再使用 `v1.1.9-p2` 的 Canvas 实验方案。
- 出入站统计和流量额度统计现在会一起跟随卡片外壳预设与渐变背板同步色彩。

## v1.1.9-p3

- 回退 `v1.1.9-p2` 中将流量额度进度条改为 Canvas 数据条绘制的实现，避免由该实验改动引发的严重显示和交互问题。
- 恢复 `v1.1.9-p1` 的稳定流量额度样式：继续保留卡片外壳预设、渐变背板同步和配色跟随能力。

## v1.1.9-p2

- 对齐方型卡片“数据条”里的流量额度细节样式：额度展示改为全宽数据条结构，标题、百分比、已用 / 总量细节更接近 CPU、内存、磁盘等数据条。
- 流量额度进度条改为复用同一套 Canvas 跑马灯绘制，支持跟随当前数据条的点阵、胶囊、波形、刻度等样式和动效。

## v1.1.9-p1

- 优化方型卡片“数据条”的流量额度块外观，降低默认状态下的独立色块感，让它更接近原有数据块。
- 流量额度块现在会跟随卡片外壳预设，并接入渐变背板的同步色彩；进度槽、边框和辅助文字会一起响应当前外观。

## v1.1.9

- 方型卡片的信息展板「数据条」新增流量额度展示，在上下行流量区域下方显示「已用 / 总量」和百分比进度。
- 流量额度会根据 `traffic_limit_type` 自动按 `sum`、`max`、`min`、`up` 或 `down` 计算已用值，没有设置流量限制时保持原样不显示。

## v1.1.8-p9

- 调整液位容器性能方案：恢复轻量动态效果，水波、水面轻晃、气泡、少量扫描/脉冲/传输点会继续运动。
- 保留上一版的性能优化方向：重型 SVG 滤镜、玻璃内层模糊、多层装饰同时动画仍然关闭，避免回到高掉帧状态。
- 液位容器动效现在更偏“轻动态”，在视觉活跃度和多节点性能之间做平衡。

## v1.1.8-p8

- 优化液位容器性能：减少每个液位小图的背层波浪、背层液面和多余气泡，降低首页多节点场景下的 SVG 节点与动画数量。
- 将液位容器的科技 HUD、轨道、扫描、节点脉冲等装饰动效改为静态显示，只保留主水波动态，减少持续重绘和掉帧。
- 移除液位容器 SVG 层的大量 `drop-shadow` 滤镜，并关闭玻璃外壳内层 `backdrop-filter`，降低 GPU 合成压力。
- 拉长液位容器动效最短周期，避免动效强度较高或数据变化较快时动画刷新过密。
- 将设置面板中的液位容器预览扫描动画改为静态纹理，减少打开外观设置时的持续动画负担。

## v1.1.8-p7

- 重做液位容器的视觉结构：为水波圆球、横向胶囊舱、竖向液柱、椭圆透镜、分段胶囊、六边晶核、悬浮水滴、环形液舱加入实体化外壳、瓶口、底座、卡扣、折射线和容器厚度。
- 强化液体表现：新增渐变液体、动态水波、液面椭圆、三组气泡和水面轻微起伏，让内存/硬盘等容量占比更像真实水位变化。
- 强化科技与赛博细节：保留并加强轨道、扫描、刻度、晶体切面、传输节点、分段点亮等 HUD 层，动效继续跟随“动效强度”。
- 液位容器现在会更明显地跟随卡片外壳变化：清透玻璃更透明，霓虹暗面更赛博发光，柔和彩块更柔和，复古 CRT 增加荧光和色散质感。
- 更新液位形态预设说明和 README 描述。

## v1.1.8-p6

- 首页快捷按钮不再单独显示“渐变背板”入口，渐变背板整体移动到“卡片与样式”面板中。
- “卡片与样式”页签调整为“卡片外壳 / 信息展板 / 渐变背板 / 配色”四项并列，渐变预设、颜色、角度、柔和、透明和网格都在新页签里调整。
- “卡片与样式”的恢复默认按钮会按当前页签恢复本机卡片样式或本机渐变背板，避免两个配置互相干扰。
- README 和 AI 交接文档同步更新新的快捷面板结构。

## v1.1.8-p5

- 将“背板玻璃”从渐变背板面板移动到“卡片与样式”里的卡片外壳区域，更符合它影响节点卡片和总览色块的使用逻辑。
- 首页快捷面板的“应用全局”现在会同时保存卡片与样式和背板玻璃配置，避免只保存部分外观。
- 主题管理页的“全站默认卡片与样式”加入背板玻璃开关和玻璃强度，“全站默认渐变背板”只保留渐变本身的颜色、角度、柔和、透明和网格设置。
- README 和 AI 交接文档同步补充背板玻璃的入口与字段关系。

## v1.1.8-p4

- 拆分主题设置页工具逻辑，将选项常量、背景上传优化、Ping 绑定处理和帧调度工具移入 `src/pages/themeManage/`。
- 拆分首页节点卡片仪表盘辅助逻辑，将弧光/全环/指针/液位展板的 SVG、数学和样式变量工具移入 `src/components/node/dashboardHelpers.tsx`。
- 拆分大型 surface 样式文件，将基础/快捷面板、详情页、节点卡片样式分到 `src/styles/surface/`，保留原导入顺序。
- 新增 `docs/AI_HANDOFF.md`，整理项目文件职责、Komari 接口、主题配置字段、常见开发入口和发版检查项，方便无上下文接手维护。
- README 增加维护文档入口。

## v1.1.8-p3

- 修正主题清单 URL 为 `https://github.com/youshi01/Komari_theme`，确保主题更新检测指向正确仓库。

## v1.1.8-p2

- 主题设置页“全站默认卡片与样式”默认收起，展开后再渲染完整卡片、仪表、跑马灯和配色设置。
- 主题设置页“全站默认渐变背板”默认收起，减少打开全局设置时的页面长度和初始渲染负担。
- 收起状态下只保留标题、说明和展开按钮，方便优先使用首页快捷面板的“应用全局”。

## v1.1.8-p1

- 优化仪表盘“断点扫描”动效：扫描拖尾不再跟随实时百分比重置，减少全环仪表刷新后掉帧和同步跳动。
- 为每张节点卡、每个仪表指标加入稳定扫描相位，让扫描动效更连续、不容易全站同拍。
- 首页“卡片与样式”快捷面板新增“应用全局”按钮，仅登录管理员可见，可将当前卡片与样式保存为全站默认。

## v1.1.8

- 优化跑马灯与历史条 Canvas 绘制：缓存颜色解析与 CSS 变量解析，降低多节点同时动画时的主线程压力。
- Canvas 动画现在会在页面隐藏或节点离开可视区域附近时暂停重绘，并限制高 DPR 下的像素绘制量，让动态衔接更稳。
- 主题管理页的跑马灯预览改为静态绘制，后台 Ping 任务和节点列表改为空闲/用户操作时再加载，减少打开全局设置时的卡顿。
- 保存主题设置时仅在背景配置变更后处理上传背景，并在大体积配置 JSON 估算超过阈值后提前短路，减少保存前后的卡顿感。

## v1.1.7

- 调整“霓虹警戒”跑马灯配色，让对比和亮度更明显。
- 新增 8 套跑马灯配色预设：赛博霓虹、电光蓝紫、落日能量、酸性绿光、等离子、熔岩警戒、矩阵绿、极光撞色。
- 新配色会同时用于首页快捷面板和主题管理页，仍然支持在预设基础上继续自定义每项指标颜色。

## v1.1.6

- 重做液位容器的科技 HUD 细节：水波圆球、竖向液柱、分段胶囊、横向胶囊舱、椭圆透镜和六边晶核加入轨道、扫描、刻度、能量线和核心脉冲。
- 新增液位面板扫光、轨道流动、数据包移动、晶核呼吸和分段闪烁等动态效果，并继续跟随“动效强度”设置。
- 优化液位容器的玻璃、发光、网格和边框质感，整体更接近概念图里的未来监控面板风格。

## v1.1.5

- 修复“同步卡片色彩”对部分卡片外壳不生效的问题；开启后节点卡片会稳定跟随当前渐变背板色彩。
- 玻璃、霓虹、柔彩、极简和复古 CRT 外壳现在都会在同步色彩开启时改用渐变背板的主色、辅色和强调色。
- 同步优化节点卡片内部指标块、条形卡片信息块、仪表/液位容器、标签和详情按钮的背景与边框，让总览和节点卡片的视觉更统一。

## v1.1.4

- 重新调整手机端条形卡片：窄屏下不再把条形内容压缩进屏幕宽度，而是使用固定宽度横向卡片，外层支持横向滑动，保留条形视觉。
- 手机端条形卡片改为左侧节点信息、右侧两排指标与状态，避免条形卡片被压成方形。

## v1.1.3

- 首页快捷按钮改为每次打开页面默认收起，避免遮挡首页数据和手机端内容。
- 优化手机端条形卡片布局：条形卡片在窄屏下不再两列排列，核心指标和状态信息改成单列横向小条，避免显示成方形卡片。

## v1.1.2

- 修复“未登录隐藏节点”在已登录状态下仍被主题隐藏的问题。`hidden` 字段现在按接口语义仅用于未登录访客过滤，管理员/已登录用户可以正常看到这些节点。
- 同步修正首页节点列表、详情页节点读取、Ping 首页概览和实时排序里的隐藏节点判断，避免不同视图表现不一致。

## v1.1.1

- 在“卡片外壳”设置中新增“卡片形态”，支持“方型卡片”和“条形卡片”两种首页节点展示方式。
- 条形卡片采用单列紧凑列表，横向展示节点身份、CPU/内存/硬盘/负载、上下行速率、延迟、丢包、到期和在线时长，适合服务器数量较多时快速扫读。
- 首页快捷面板与主题管理页均支持保存卡片形态，本机外观和管理员全站默认都可独立生效。
- 条形卡片同步适配数据面板、清透玻璃、霓虹暗面、柔和彩块、极简白板和复古 CRT 外壳，并补充响应式布局。

## v1.1.0-p9

- 优化主题管理页「首页服务器排序」：自定义服务器顺序列表默认收起，只显示摘要、恢复默认排序和展开编辑按钮，避免服务器较多时页面过长。
- 展开自定义排序后仍保留搜索、拖拽排序、置顶、上移、下移和置底操作。
- 优化「主页延迟检测」绑定：每个 Ping 任务新增「应用全部节点」按钮，可一键将所有服务器绑定到该任务，并自动从其他任务移除。
- 已绑定全部服务器的 Ping 任务会禁用一键应用按钮，减少误操作。

## v1.1.0-p8

- 重做液位容器的个性层，让 8 种形态不再只是换轮廓，而是带有不同的未来 HUD 视觉。
- 水波圆球新增轨道环、扫描扇面和节点脉冲；横向胶囊舱新增双轨传输、能量包和信号折线。
- 竖向液柱新增刻度、能量脊线和上升脉冲；椭圆透镜新增全息网格、准星与扫描光。
- 分段胶囊新增状态格、流动中线和传输点；六边晶核新增晶体切面、核心多边形和节点光。
- 悬浮水滴新增双轨环绕与内部星形能量；环形液舱新增内外轨道、方向信标和节点脉冲。
- 优化液位预览和动画兼容性，纹理强度会影响科技层明显程度，关闭系统动画时同步停用新动效。

## v1.1.0-p7

- 为信息展板「液位容器」新增 8 种形态：水波圆球、横向胶囊舱、竖向液柱、椭圆透镜、分段胶囊、六边晶核、悬浮水滴、环形液舱。
- 首页快捷面板和主题管理页均支持调整液位容器形态，并可配置波浪高度、玻璃强度、光晕强度、动效强度和纹理强度。
- 光晕强度与动效强度支持 200% 上限，和环形仪表的增强区间保持一致。
- 优化液位容器的 SVG 外壳、高光、分段线、环形核心和预览反馈，让不同形态的视觉差异更明显。

## v1.1.0-p6

- 新增信息展板「液位容器」，CPU、内存、硬盘、负载、上下行、延迟和丢包统一使用水波液位球展示。
- 液位容器中心显示当前映射百分比，底部保留真实数值；上下行会按当前速率单位自动映射到合适上限，延迟继续使用可配置上限。
- 新增卡片外壳「复古 CRT」，包含暗色荧光屏、扫描线、暗角和轻微色散质感，内部指标块与液位球同步适配。
- 更新首页快捷面板与主题管理页预览，让液位容器和复古 CRT 在选择时有对应视觉反馈。

## v1.1.0-p5

- 新增 8 个个性环形样式：碎片轨道、脉冲齿环、液态胶囊、电路星轨、声波脉冲、双轨错位、极光丝带环、断点扫描。
- 弧光仪表、全环仪表和指针仪表共用这套个性环形形态，进度头、断点、齿环、节点、声波柱和扫描拖尾会随实时百分比动态变化。
- 优化环形样式选择面板为紧凑两列，样式变多后首页快捷面板和主题管理页仍然可滚动选择。
- 修正首页快捷面板中环形样式列表的紧凑布局位置，避免误作用到数据条样式列表。

## v1.1.0-p3

- 将弧光仪表、全环仪表和指针仪表的光晕强度、动效强度上限提升到 200%，保留 0-100 的原有调节手感，100-200 作为增强区间。
- 新增清透光环、霓虹双轨、分段刻度、柔光厚环、极简细线 5 组环形样式预设，弧光、全环和指针仪表共用同一套样式体系。
- 首页快捷面板和主题管理页均支持选择环形样式，管理员保存后可作为全站默认外观。

## v1.1.0-p2

- 为弧光仪表新增专属设置：弧线粗细、弧光强度、动效强度和紧凑度。
- 为全环仪表新增专属设置：环宽、中心数值大小、光晕强度和动效强度。
- 为指针仪表新增专属设置：弧线粗细、指针粗细、刻度强度、光晕强度和动效强度，并补充仪表刻度显示。
- 首页快捷面板与主题管理页同步使用同一套信息展板专属设置，保存后可作为本机样式或全站默认样式生效。

## v1.1.0-p1

- 调整首页「卡片与样式」快捷面板，顶层改为「卡片外壳 / 信息展板 / 配色」三组。
- 将跑马灯样式与动态细节整合进「信息展板 -> 数据条」的专属设置区，避免误以为跑马灯会影响弧光、全环和指针仪表。
- 信息展板设置改为左侧选择样式、右侧显示当前样式细节；仪表类暂保留延迟上限设置，后续可继续扩展各自专属参数。
- 主题管理页同步将数据条跑马灯设置收进信息展板配置块。

## v1.1.0

- 重构首页「卡片与样式」快捷面板，拆成「卡片外壳 / 信息展板 / 跑马灯 / 配色」四个横向分组，减少单个面板过长的问题。
- 将卡片外壳与信息展板拆分为两套独立配置，支持外壳、展板、跑马灯和配色自由组合。
- 信息展板新增「数据条、弧光仪表、全环仪表、指针仪表」四种样式，并保留延迟上限调节。
- 将旧配置里的 `cardStyle: "radar"` 自动迁移为「数据面板 + 弧光仪表」，避免已有用户样式失效。
- 同步更新主题管理页的全站默认样式设置与预览，方便管理员保存默认展示效果。

## v1.0.24

- 优化「3号雷达」卡片布局，仪表盘改为每行 2 个，减少底部真实值拥挤。
- 同步调整主题设置预览中的雷达样式为 2 列展示。

## v1.0.23

- 修正「3号雷达」卡片底部信息：CPU 显示核心数，内存和磁盘显示已用/总量，不再重复显示百分比。
- 优化雷达卡片底部长文本显示，避免内存、磁盘容量文本撑开布局。

## v1.0.22

- 优化「3号雷达」卡片展示：仪表中心统一显示开合百分比，底部显示真实数值。
- 隐藏雷达卡片底部的上限文案，上限仅保留在悬停提示中，界面更清爽。

## v1.0.21

- 新增卡片样式「3号雷达」，节点信息展板改用 180 度半圆仪表展示 CPU、内存、磁盘、负载、上行、下行、延迟和丢包。
- 雷达仪表按 0-100 份映射开合角度，实时数据变化时会动态刷新。
- 上传/下载雷达上限按当前速率单位自动切换：bps/Kbps 使用 1 Mbps，Mbps 使用 1 Gbps，Gbps 使用 1 Tbps。
- 新增「延迟上限」设置，默认 1000ms，可在首页快捷面板和主题管理页调整，超过上限时仪表拉满。

## v1.0.20

- 移除未使用的 `framer-motion` 依赖，减少安装体积与依赖维护面。
- 更新 `react-router-dom` 与 `vite` 安全补丁版本，修复此前 audit 中的 high 级别提示。
- 更新 `@babel/core` 传递依赖，并通过 `overrides` 固定 `esbuild` 到安全版本，`npm audit` 已清零。

## v1.0.19

- 优化延迟/丢包小图的 canvas 性能，没有有效点时不再订阅动画。
- 优化丢包兜底显示，只有当前丢包值时改为右侧少量断点，不再整排亮起。
- 动画 canvas 首帧立即绘制，减少加载时短暂空白。
- 跑马灯绘制中每帧只读取一次根样式，降低多节点页面的重复开销。

## v1.0.18

- 延迟历史条改为同一色系深浅变化，高延迟更深、低延迟更浅。
- 降低延迟与丢包历史条的动效强度，保持轻微动态但不影响读数。
- 丢包历史改为断点表达，只有出现丢包的位置才点亮。

## v1.0.17

- 修复极光丝带样式下同时出现丝带与点阵/短块叠加的问题。
- 保留原跑马灯预设体系，只优化极光丝带的视觉层次。

## v1.0.16

- 新增跑马灯动态样式预设：经典点阵、极光丝带、电路线条、霓虹脉冲、均衡器方块。
- 首页快捷面板与主题管理页均支持调整跑马灯样式、密度、圆角、光晕和动效强度。
- CPU、内存、磁盘、负载、上下行、延迟、丢包历史条统一接入动态跑马灯绘制。
- 使用共享 canvas 动画时钟，减少多节点场景下的动画调度开销。

## v1.0.15

- 优化首页快捷菜单交互，点击面板外区域会自动收起已打开菜单。

## v1.0.14

- 新增在线时长排序。
- 优化卡片与跑马灯设置面板，内容较多时支持滚动操作。
